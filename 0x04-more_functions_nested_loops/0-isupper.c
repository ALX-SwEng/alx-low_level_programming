#include "main.h"

/**
 * _isupper -  Make a triangle with #
 * @c: character
 * Return: 1 if c is uppercase 0.
 */
int _isupper(int c)
{
    	if (if c >= 'A' && c <= 'Z')
		return (1);
    	else
		return (0);
}
