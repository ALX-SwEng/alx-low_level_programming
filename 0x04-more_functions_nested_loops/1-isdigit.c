#include "main.h"

/**
 * print_alphabet -  Make a triangle with #
 * @c: data
 * Return: 1 if c is digit otherwise 0.
 */
int _isdigit(int c)
{
    	if (if c >= 48 && c <= 57)
    	{
        	return (1);
    	}
	else
	{
		return (0);
    }
}
